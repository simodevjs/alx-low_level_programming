#include "main.h"

/**
 * _isupper - checks passed char is uppercase
 * @c: the char to be checked
 * Return: 1 if uppercase, 0 otherwise
 */
int _isupper(int c) {
    return (c >= 'A' && c <= 'Z');
}
